env_name: "thesis-env-v1"
gamma: 0.99
n_epochs: 15000
seed: 123
device: "cpu"
test_interval: 500 # set to null to disable testing
test_n_epochs: 20
nn:
  policy: null # Unused for now
  actor_arch: [350, "relu", 350, "relu"]
  critic_arch: [350, "relu", 350, "relu"]
  max_grad_norm: null # Unused for now
  batch_size: 20
  train_iters: 50
  optimizer_act: "Adam"
  lr_act: 1.0e-5
  lr_act_min: 1.0e-7
  optimizer_crit: "Adam"
  lr_crit: 1.0e-4
  lr_crit_min: 1.0e-6
  lr_decay_step: 200
  lr_decay_episode_perc: 0.9
  checkpoint_dir: "src/model_weights/"
  checkpoint_interval: 250
ppo:
  clip_range: 0.2
  clip_range_vf: null
  normalize_advantage: True
  ent_coef: null
  vf_coef: null # Unused for now
  target_kl: 0.01
  lam: 0.92
plot: # TODO
  enabled: False
  update_interval: 20000
  reset_heat_map_every_update: True
  # Configure smoothing per plot
  smooth:
    # Set to True to enable plot smoothing in general
    enabled: False
    # Plots to enable
    episode_rewards: True
    total_rewards: True
    n: 11
log: # TODO
  enabled: True
  save_interval: 2000

 